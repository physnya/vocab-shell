[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vocab-shell"
version = "0.1.0"
description = "Terminal vocabulary trainer for TOEFL preparation"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["vocab_shell"]
